#include <stdio.h>
#include <stdarg.h>

void print_all(const char * const format, ...)
{
	va_list list;
	unsigned int j = 0, start = 0;
	char *p;

	
}

int main(void)
{
    print_all("ceis", 'B', 3, "stSchool");
    return (0);
}
